@layout('main')
@section('content')

<h1>{{ title }}</h1>
@if(old('notification'))
  <div class="alert alert-success">
    {{ old('notification')}}
  </div>
@endif
<ul class="list-grou">
  @each(post in posts)
    <li class='list-group-item'><a href='/posts/{{ post.id }}'> {{ post.title }}</a> - {{ post.body }}</li>
  @endeach
</ul>

@endsection
